{
  "name": "history-ts",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node --enable-source-maps ./dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p ."
  },
  "dependencies": {
    "dotenv": "*",
    "fastify": "*",
    "mongodb": "*"
  },
  "devDependencies": {
    "@types/dotenv": "*",
    "ts-node-dev": "*",
    "typescript": "^5.9.2"
  }
}
